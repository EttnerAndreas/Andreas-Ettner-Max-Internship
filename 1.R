install.packages("git2r")

library(git2r)

2+2
a <- c("1", "2", "3", "4")
a
git config --global user.email "you@example.com"
git config --global user.name "Your Name"
git_congi
git config --global user.name "andreasettner"
git config --global user.email "andi.ettner@yahoo.de"


?user.name

x <- system("whoami", intern=TRUE)
x
email = andi.ettner@yahoo.de
